<form method="post" action="{{ path('app_admin_writing_delete', {'id': writing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ writing.id) }}">
    <button class="btn" style="margin:1vh; margin-left:20%;">Supprimer</button>
</form>
