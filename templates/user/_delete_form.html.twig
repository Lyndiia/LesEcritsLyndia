<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Est-tu sur de vouloir supprimer ton compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn" style="margin-left: 25%; margin-top:3vh;">Effacer mon compte</button>
</form>
